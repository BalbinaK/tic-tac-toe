window.addEventListener('click', function(event) {
    event.target.classList.add('x');
    
    //var clickedElement = event.target
   // if (clickedElement.classList.contains('taken')) {
   //   return;
   // }
   // clickedElement.classList.add('x');
})